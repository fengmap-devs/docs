(function (global, factory) {
typeof exports === 'object' && typeof module !== 'undefined' ? module.exports = factory() :
typeof define === 'function' && define.amd ? define(factory) :
(global = global || self, global.duanliang = factory());
}(this, (function () { 'use strict';

function _classCallCheck(instance, Constructor) {
  if (!(instance instanceof Constructor)) {
    throw new TypeError("Cannot call a class as a function");
  }
}

function _defineProperties(target, props) {
  for (var i = 0; i < props.length; i++) {
    var descriptor = props[i];
    descriptor.enumerable = descriptor.enumerable || false;
    descriptor.configurable = true;
    if ("value" in descriptor) descriptor.writable = true;
    Object.defineProperty(target, descriptor.key, descriptor);
  }
}

function _createClass(Constructor, protoProps, staticProps) {
  if (protoProps) _defineProperties(Constructor.prototype, protoProps);
  if (staticProps) _defineProperties(Constructor, staticProps);
  return Constructor;
}

var version = "1.0.0";

var Map = function Map() {
  var options = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};

  _classCallCheck(this, Map);

  if (!window.fengmap) throw '未找到Fengmap SDK';
  /**
   * options不传入任何参数则默认开启10347在线地图；
   */
  // options = {
  //     container: document.getElementById('fengMap'),
  //     mapId: '10347',
  //     appName: '蜂鸟研发SDK_2_0',
  //     key: '57c7f309aca507497d028a9c00207cf8',
  //     ...options
  // }

  var fmMap = new fengmap.FMMap(options);
  fmMap.openMapById(options.mapId, function (error) {});
  var imageMarkerLayer = new fengmap.FMImageMarkerLayer();
  this.fmMap = fmMap;
  this.imageMarkerLayer = imageMarkerLayer;
};

var Marker = /*#__PURE__*/function () {
  function Marker() {
    _classCallCheck(this, Marker);
  }

  _createClass(Marker, [{
    key: "addMarker",
    value: function addMarker() {} // //获取当前聚焦楼层
    // var group = map.getFMGroup(map.focusGroupID);
    // /*//实例化方法1：自定义图片标注层
    //  layer = new fengmap.FMImageMarkerLayer();
    //  //添加图片标注层到模型层
    //  group.addLayer(layer);*/
    // //实例化方法2：
    // //返回当前层中第一个imageMarkerLayer,如果没有，则自动创建
    // layer = group.getOrCreateLayer('imageMarker');
    // //图标标注对象，默认位置为该楼层中心点
    // var gpos = group.mapCoord;
    // im = new fengmap.FMImageMarker({
    //     //标注x坐标点
    //     x: gpos.x,
    //     //标注y坐标点
    //     y: gpos.y,
    //     //设置图片路径
    //     url: './images/blueImageMarker.png',
    //     //设置图片显示尺寸
    //     size: 32,
    //     //标注高度，大于model的高度
    //     height: 4
    // });
    // im.selfAttr = '自定义属性selfAttr';
    // layer.addMarker(im);

  }]);

  return Marker;
}();

var Fmap = function Fmap(options) {
  _classCallCheck(this, Fmap);

  var map = new Map(options);
  var fmMap = map.fmMap;
  Marker.prototype.map = fmMap;
  this.map = fmMap;
};

var index = {
  version: version,
  Fmap: Fmap,
  Marker: Marker
};

return index;

})));
